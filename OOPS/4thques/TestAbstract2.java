package com.ashna.oops;

public /*abstract */ class TestAbstract2 /* extends TestAbstract */ {
	
	//Here if we remove the comments we see that we are getting an error. 
	//It is because if we extent an abstract class we must override all its abstract method
	//or declare the subclass also as abstract. 
	
	//As we can see if we remove the comments we get an error
	//because we cannot create an object for an abstract class. 
	
	/*
	TestAbstract test= new TestAbstract();
	*/

}
